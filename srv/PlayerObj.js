class PlayerObj {
    constructor(id, name) {
        // the playerId
        this.id = id;

        // the players name
        this.name = name;

        // the players ready state
        this.ready = false;
    }
}

export default PlayerObj;
